{
  "name": "cataclysm-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitest": "4.1.11",
    "wrangler": "4.128.0"
  }
}
